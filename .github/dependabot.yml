version: 2
updates:
  # Maven dependencies (Paper API, shaded libs, test deps, build plugins)
  - package-ecosystem: maven
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5

  # Actions used by build.yml / release.yml
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
